<UserControl
    x:Class="AveoAudio.Views.QueueView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:AveoAudio.ViewModels"
    xmlns:views="using:AveoAudio.Views"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400"
    VerticalContentAlignment="Stretch"
    x:Name="view">

    <Border Background="{Binding Background, ElementName=view}">
        <views:TracklistView DataContext="{x:Bind ViewModel}" Margin="20 20 10 20" />
    </Border>
</UserControl>
