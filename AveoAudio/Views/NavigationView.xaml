<UserControl
    x:Class="AveoAudio.Views.NavigationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="view">

    <StackPanel Background="Transparent"
                ManipulationMode="TranslateX"
                ManipulationCompleted="OnManipulationCompleted">
        <StackPanel Visibility="{x:Bind ViewModel.CanGoBack, Mode=OneWay}">
            <StackPanel Orientation="Horizontal">
                <Button
                    Click="{x:Bind ViewModel.GoBack}"
                    Width="{StaticResource TileSize}"
                    Style="{StaticResource NavigationBackButtonSmallStyle}" />

                <TextBlock VerticalAlignment="Center" Margin="20 0"
                           Text="{x:Bind ViewModel.Title, Mode=OneWay}" />
            </StackPanel>
            <Border BorderBrush="LightGray" BorderThickness="1" />
        </StackPanel>

        <ContentControl Content="{x:Bind ViewModel.CurrentView, Mode=OneWay}"
                        HorizontalContentAlignment="Stretch"
                        IsTabStop="False" />
    </StackPanel>
</UserControl>
